{
  "name": "ccp6-project.assessment",
  "version": "1.0.0",
  "description": "This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).",
  "main": "index.js",
  "dependencies": {
    "body-parser": "^1.20.1",
    "bootstrap": "^5.2.2",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.18.2",
    "knex": "^2.3.0",
    "pg": "^8.8.0",
    "postgres": "^3.3.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "cd backend && npx nodemon server.js",
    "build": "npm install && npm run migrate:latest && cd frontend && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate:latest": "npx knex migrate:latest --knexfile backend/db/knexfile",
    "migrate:down": "npx knex migrate:down --knexfile backend/db/knexfile",
    "seed:data": "npx knex seed:run --knexfile backend/db/knexfile.js"
  },
  "author": "",
  "license": "ISC",
  "optionalDependencies": {
    "fsevents": "^2.3.2"
  }
}
